@AGENTS.md

## Git push instructions

Always push changes to **both** remotes:
1. `git push origin main` — Amvera remote (local proxy)
2. Push to GitHub via `mcp__github__push_files` tool to `vladilslavandrosov-prog/job-3` branch `main`

GitHub is the primary trigger for Amvera auto-rebuild.

