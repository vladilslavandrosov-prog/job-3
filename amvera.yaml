meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build
  artifacts:
    "*": /

run:
  scriptName: sh
  scriptArguments: start.sh
  containerPort: 3000
  persistenceMount: /data
