meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build && pip install -q -r bot/requirements.txt
  artifacts:
    "*": /

run:
  scriptName: sh
  scriptArguments: start.sh
  containerPort: 3000
  persistenceMount: /data
